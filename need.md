## 需求描述

开发一个习惯打卡微信小程序，对标 TapHabit 这类习惯追踪 App，完整需求如下：

### 核心功能

1. **习惯（打卡项）管理**

- 用户可以**自定义添加打卡类型**：自定义名称、自定义单位（单词 / 公里 / 卡路里 / 次数 / 毫升等）
- 每个打卡项支持绑定**数值变量**：打卡时可以填写数字，例如背单词填写个数、骑行填写公里、喝水填写毫升、GitHub 提交次数
- 支持编辑、删除已创建的打卡项目；可以自主开关该习惯。

2. **每日打卡功能**

- 日历视图，选择日期打卡；
- 打卡表单：支持填写自定义数值；可以重复多次打卡（同一天多条记录）；
- 打卡记录保存在小程序本地缓存 storage，数据存在用户手机，不上云；
- 连续打卡天数统计，断签会重置连续计数。

3. **多维度统计面板**

- 时间维度：**周视图、月视图、年视图**切换；
- 汇总指标：总次数、累计变量数值（总单词数 / 总骑行公里等）、连续打卡天数；
- 可视化图表：
  - 热力点阵日历图（类似习惯格子，标记哪些天完成打卡）
  - 折线图：按日展示每日数值变化
  - 柱状图：周 / 月对比统计

> 
> 使用小程序生态可用图表库（ucharts）渲染图表。

4. 页面结构

- 首页仪表盘：所有习惯卡片，点阵热力概览；
- 习惯详情页：单习惯打卡、历史记录、图表统计；
- 统计总览页：周 / 月 / 年汇总数据；
- 添加习惯弹窗：填写习惯名称、单位。

### 技术约束

1. **无服务端，不使用 Java 后端，全部数据持久化到微信小程序 wx.storage 本地存储**，数据只保存在用户手机；
2. 原生微信小程序，不使用 uniapp；技术栈：wxml + wxss + js；
3. UI 风格：深色暗黑模式，简约现代，圆角卡片，和 TapHabit 风格接近；
4. 页面底部 tab 导航：首页仪表盘｜统计图表｜个人设置；
5. 代码需要写注释，模块拆分清晰；需要考虑本地存储的数据结构设计；
6. 处理边界：删除习惯同步删除对应打卡记录；本地存储溢出提示；日期选择；周月年切换计算。