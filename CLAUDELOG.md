# CLAUDELOG.md

> 本文件记录 TapHabit 习惯打卡小程序的版本变更日志与关键决策记录，供 Claude Code / AI 助手在修改本项目时参考历史。
> 面向用户的介绍见 `README.md`，开发约定见 `CLAUDE.md`。

---

## 目录

- [版本记录](#版本记录)
- [关键决策记录](#关键决策记录)
- [历史遗留与已移除功能](#历史遗留与已移除功能)
- [已知坑与踩坑记录](#已知坑与踩坑记录)

---

## 版本记录

> 更新提示每次打开小程序都弹一次「已更新至 vX.Y.Z」并列出本次更新内容。
> 勾选「此次更新不再显示」只压制**这一个版本**，下次改版本号又会弹。
> 更新日志页面只铺最近 1 条，更早的不展开，点「更早的版本记录」复制仓库地址。

### 当前版本

- 版本号：见 `utils/version.js`
- `th:meta.appVersion` 记着上次打开时的版本号，供排查问题时对照

---

## 关键决策记录

### 液态玻璃只覆盖浅色 / 深色两套主题（1.3.0）

- **背景**：整套界面换成「液态玻璃」，但**不能碰「自定义主题」那套配色**（用户明确要求只改系统主题）
- **做法**：所有新装饰都是**别名 token**，`app.wxss` 的 `page{}` 里默认值要么写死成「改动前的原值」（`--glass-card: var(--surface)`、`--glass-edge: var(--border-soft)`），要么是空操作（`--glass-shadow: none`、`--glass-press: 1`）
- **关键坑**：CSS 变量是在**声明它的那个元素**上求值的，所以 `.sheet` / `.modal` 这些会重绑 `--surface` 的容器必须**把玻璃别名一起重绑**，否则弹层里的卡片拿到的是页面级的值
- **边界**：`buildCustom()` 有一份自己的返回键清单，新键只加进 `THEMES.light` / `THEMES.dark`，自定义主题自然落到默认值上 —— 加玻璃时别往 `buildCustom` 里加
- **例外**：卡片毛玻璃的饱和度是 `--glass-sat` 这个 token，不是写死的 180%。自定义主题的卡片也能调透明度，写死会把那一套一起改掉

### 底栏选中块最终**不做动画**（1.3.0）

- **结论先行**：选中块直接出现在目标段，`.tab-pill` 上没有 transition。**外观照旧**（还是那层玻璃），只是不动
- **走过三版，都是死路**：
  1. 滑块滑 320ms 而页面 140ms 就切走了，跨实例靠「按已过去的时间插值」接续 —— 老实例滑到一半没了
  2. 滑动与淡出「同时开始、时长相等」，到期才切页 —— **依旧抽搐**：两件事抢同一段时间，滑块到位那一刻正好撞上切页
  3. 改成三段**串行**（滑完 → 图标才亮 + 才淡出 → 淡完才切页），总共 340ms —— 用户反馈「最终还是搞不好」，放弃
- **根因**（改多少版都绕不过去）：每个 tab 页各有一个 tabBar **实例**，切页时老实例被整个丢掉，新实例重新挂载。跨实例的动画必然在切页那一帧断掉；「滑完再切页」只是把抽动摊长成延迟
- **留住的**：几何 `rects` 还是模块级缓存（三实例共用，量一次长期有效）；配色按主题名缓存（同一主题下 `tabbarVars` 是纯函数，新实例不必再 setData 六枚 SVG）；`attached` 里按当前路由定 `selected` 初值，避免第一帧画错
- **别回头**：不要再给 `.tab-pill` 加 transition，也不要再引入「先滑后切」的定时器。`transform` 也不要用（会把 fixed 胶囊提成独立合成层，滚动后点不动）

### 开关 / 滑块自绘，替掉原生组件（1.3.0）

- **原因**：`<switch>` / `<slider>` 是**原生组件**，WXSS 完全够不着，做不出玻璃材质
- **做法**：`components/glass-switch` / `components/glass-slider`，对外接口**照抄原生** —— 同名属性（`checked` / `color` / `min` / `max` / `step` / `value` / `activeColor`）、`e.detail.value`、组件节点上的 `data-*` 照样出现在 `e.currentTarget.dataset` 里。所以页面只把标签名换掉，处理函数一行都不用改
- **滑块的矩形只在 `touchstart` 量一次**，整段拖动复用：每帧 `createSelectorQuery` 会明显掉帧
- **连带**：两处 `.sl-row slider { margin: 0 -8rpx }` 是给**原生** slider 抵消自带留白的，换成自绘之后那层留白没了，负边距反而把滑块顶出卡片 —— 已删，注释留在原处

### 玻璃让底色变浅之后，热力图与开关「关态」看不出来了（1.3.0）

- **起因**：卡片一变成半透明，底下那层彩光就透上来 —— 两处「照不透明卡片挑的颜色」当场失效（用户反馈「可见性被下降，得不偿失」）
- **热力图**：「没打卡」那一格原来是实色 `--heat-0`（浅色 `#EBEDF1`），贴着半透明卡片几乎同色，整片网格看不见了 → 新增 `--heat-empty`，浅色是 `rgba(18,21,28,0.10)`、深色是 `rgba(255,255,255,0.10)`：**半透明**的色落在任何底色上都还在。浅色的 `--heat-1` 也顺手深了一档（1.2:1 和没打卡分不出来）
- **写法**：`--heat-empty` 也是别名 token，`app.wxss` 的 `page{}` 里默认 `var(--heat-0)` —— 自定义主题的卡片不透明，取到老值，和改动前一样
- **坑**：格子的颜色是 `heatmap/index.js` 的 `fillOf()` 拼**行内样式**给的，改 wxss 没用，得连着 JS 和图例（`heatmap/index.wxml`）一起改
- **开关**：「关着」那条轨道原来走 `--glass-soft`，浅色下是半透明白，落在浅底上等于没画 → 新增 `--glass-track` / `--glass-track-edge`，浅色是一整条实灰轨 + 白描边，深色仍是半透明白。描边是 border-box 里的，旋钮行程要从 52rpx 改成 48rpx

### 分段控制器也滑动，还带上演示那层「背景扭曲」（1.3.0）

- **范围**：统计页 / 详情页的 日 周 月 年，设置页的 浅色 深色 跟随系统 自定义
- **做法**：和底栏同一套 —— 选中那块玻璃是 `.segment::before`（独立一层），位置只由页面上写在 `.segment` 里的行内 `--seg-i` 决定，宽度与步长按 `--seg-n`（默认 4）在 `app.wxss` 里算
- **为什么不用 JS 量**：四段等宽（都 `flex: 1`），`calc((100% − 12rpx − (n−1)×6rpx) / n)` 就是每段的位置，比 `createSelectorQuery` 便宜也好读
- **扭曲**：滑块身后那层 `--glass-warp`（`blur(9px) saturate(190%) brightness(1.06)`）。演示里是 `blur(44px) saturate(220%)`，这里刻意取小 —— 它是跟着动画每帧重算的一层，44px 在低端机上就是掉帧
- **连带**：设置页那段「自定义」原来是 `flex: 0 0 76rpx`（比别的段窄），滑块按 n 等分算就落不到格上 —— 改成 `flex: 1`，圆本身还是 68rpx，只是居中放着

### 彩色背景用 `.page::before`，不加节点（1.3.0）

- **做法**：`position: fixed` 铺满视口、`z-index: -1`，四个大半径径向渐变（蓝 / 紫 / 青 / 粉）。**静态、无动画、无 `filter: blur`** —— 一次绘制缓存，滚动不重绘；动画 + 大半径模糊是每秒重绘整屏
- **为什么 fixed**：卡片滑过去时背后的光是「透出来的」才像玻璃；贴在 `.page` 自己身上，长页面一屏之后就没光了
- **为什么伪元素**：不占节点；而且必须在 `.page` **里面**才会跟着页面一起淡出（同 `app-bg`，靠 `.page` 是层叠上下文才画得对）
- **深色那套故意没有开得比浅色更亮**（两套的数值其实差不多）：深色下卡片只加 14% 的白，光斑一高，卡片上的 `--text-3` 那一级次要文字就掉到 3:1 以下。可读性优先

### 按钮的按下是**放大**，不是缩小（1.3.0）

- 演示里 `.liquid.pressing` 是 `scale(1.06)`（玻璃被挤胀），照着做了
- 但大块不能跟着放大：**全宽卡片放大 6% 会顶出屏幕左右边距**，所以整张卡片走 `--glass-press-card`（0.99）
- 手感：按下 `transform 0.1s ease-out` 跟手，松手 `0.4s cubic-bezier(.2, 1.5, .4, 1.2)` 带过冲弹回

### 图表单位：粒度就是区间

- **曾经**：粒度是「比区间细一档」的梯子
- **问题**：控件与横轴说的不是一件事——点了「周」，横轴上却是一天一根柱子
- **现在**：粒度**就是**区间（日→天、周→周、月→月、年→年）
- **实现**：靠 `date.windowOf()` 铺一串同类单位（日 30 格 / 周 26 格 / 月 12 格 / 年 5 格），不是一个区间画一格

### 图表横向滑动：用 uCharts 自带的 `enableScroll`

- **曾经考虑**：套 `scroll-view`
- **问题**：`scroll-view` 会把 Y 轴和刻度一起推走
- **现在**：用 uCharts 自带的 `enableScroll`，uCharts 只对绘图区做 `translate`，刻度钉在原地

### 对比图横轴方向：从**左**起步

- **原因**：横轴按打卡次数降序排
- **问题**：默认的 `right` 是给时间轴用的，用在这儿会把第一名顶到屏幕外
- **现在**：`scrollStart="left"`

### 去掉 `labelCount`

- **问题**：uCharts 的抽稀拿 `labelCount` 当「一屏想放几个」，且**不看 `enableScroll`**：配上 `labelCount: 6`，6 个习惯起就每隔一个名字抹白一个
- **现在**：去掉它，`maxXAxisListLength` 默认取类目总数、`ratio` 恒为 1，一个都不抽

### `chartVars()` 展平 alpha

- **问题**：uCharts 的 `hexToRgb()` 只认 hex，喂 `rgba()` 会在触摸回调里抛异常
- **现在**：`chartVars()` 先把带 alpha 的值展平成实心 hex

### 首页方形卡片：`padding-bottom: 100%` 而非 `aspect-ratio`

- **问题**：老 iOS 的 WebView 不认 `aspect-ratio`，卡片会压成一条
- **现在**：`height: 0` + `padding-bottom: 100%`（百分比内边距按**宽度**解析）

### 首页卡片：列宽 `calc((100% - 20rpx) / 2)`

- **问题**：写 50% 会把间距也算进两列，第二列被挤到下一行
- **现在**：`calc((100% - 20rpx) / 2)`

### 首页卡片：表头和格子列宽**由构造**一致

- **问题**：两个 flex 容器各写一份「项宽 + 间距 + 居中」——两份独立的账差一像素，七列下来就错开小半格
- **现在**：两边共用 `.hc-cal` 上的 `--cal-w`（7 × 27 + 6 × 14 = 273rpx），项一律 `flex: none`，于是第 i 列在两边都必然落在 `i × 41rpx`

### 首页卡片：一周从周日排

- **原因**：卡片上是「日历」，日历就是周日起排的
- **实现**：`utils/date.js` 的一周起点是周一（详情页那张能点的日历跟着它）。**不动共享的 `monthCalendar`**，在 `pages/index` 的 `cardCalendar` 里把整串右移一格：开头空 `(leadMon + 1) % 7` 格
- **保证**：行数不会因此变多（最坏仍是 6 行），卡片是按 6 行留的高度

### 卡片热力图尺寸 27rpx：倒着算

- **思路**：这块是卡片主角，先把高度用满
- **计算**：正方形卡片去掉 14rpx 内边距 / 44rpx 头部 / 52rpx 底部，剩 309rpx —— 6×27 + 5×3 + 周几那行 = 198rpx，加 12rpx 上边距共 210rpx，44 + 210 + 52 = 306 ≤ 309
- **结论**：27rpx 是**高度**允许的上限，再大一行就顶出去
- **几何必然**：格子被高度定死之后 7 列铺不满卡片宽度（273 < 309），所以整块居中而不是把圆点拉成椭圆

### 主题预设：`PRESET_KEYS` 由 `theme.DEFAULT_CUSTOM` 推出

- **问题**：手抄一份清单的话，以后往调色板里加一项，老预设会悄悄少掉那一项（切回去只觉得「哪儿不太一样」，说不上来），而这种漏只有真机上才看得见
- **现在**：`PRESET_KEYS = Object.keys(theme.DEFAULT_CUSTOM)` —— 由「可定制项」的定义直接推出来，不手抄一份清单

### 背景图：引用计数

- **原因**：一个预设存的是自定义主题里**能调的全部**，于是背景图有了多个持有者
- **现在**：删图走 `storage.releaseImageFile` 的引用计数，没人指着了才真删
- **顺序**：「换图 / 移除 / 恢复默认 / 删预设」都得**先落盘再释放**
- **整批收掉**：只有「清空数据 / 导入覆盖」这种整份设置都要没了的场合才整批收掉（`releaseBackgroundImage`，模块私有）
- **上限**：当前一张 + 三个预设各一张

### 取景台：套一层 `width: 68%` 的框

- **原因**：必须套在外层，因为高度是 `padding-bottom` 百分比撑出来的，百分比内边距按**包含块的宽度**解析，所以框一窄宽高会一起等比缩，里面百分比定位的小窗分毫不差
- **位置**：取景台收进正中的「调整背景图」弹窗，页面里不再有可拖的取景框

### 取景弹窗：量两次舞台

- **原因**：打开时量两次：立刻量一次让拖动可用，展开动画（`scale(0.94)` → 原位）走完再量一次，否则量到的是缩放后的宽度

### 导入含背景图的备份：只搬颜色不搬图

- **原因**：路径是导出那台手机的，本机打开是坏的
- **现在**：当前配置和**每个预设**的 `image` 都要抹掉，漏一个就是个空路径

### 更新提示：每次打开都弹

- **曾经**：不像早先那样「弹出即记录版本」
- **现在**：弹是常态，压制只由用户勾选决定
- **两道闸**：
  - `_updateChecked` 让一次启动只判一次（首页是 tab 页，不加就会被每次 `onShow` 弹一遍）
  - `th:settings.updateMuted` 存用户勾过「不再显示」的那个版本号，所以只压制这一个版本
- **落盘时机**：「不再显示」是在**关闭**那一刻才落盘的，且每次弹出都从没勾开始；勾了又取消、或勾完直接点「知道了」都不会留下痕迹

### 底栏「新建习惯」：已经在首页就直接调 `onAddHabit`

- **问题**：切到当前 tab 框架不会重跑 `onShow`，那个 pending 标记会一直躺着，等用户哪天切到别的 tab 再回来时突然弹出来
- **现在**：已经在首页就**直接**调页面的 `onAddHabit`，不走 `switchTab`；在别的 tab 上才走「打全局标记 + 切回首页」那条老路

### 切 tab 的整页闪烁

- **现在**：淡出 → `switchTab` → 淡入；`.page--out` 默认就在，框架提前亮出的那一帧是透明的

---

## 历史遗留与已移除功能

### 补打卡

- **状态**：功能已移除，不再提供入口与文案

### 早先的「弹出即记录版本」

- **状态**：已移除。不点遮罩就杀掉小程序不再记录版本
- **原因**：现在弹是常态，压制只由用户勾选决定

---

## 已知坑与踩坑记录

### 图表相关

- uCharts 的 `hexToRgb()` 只认 hex，喂 `rgba()` 会在触摸回调里抛异常
- `scroll-view` 会把 Y 轴和刻度一起推走
- 重画时 `xAxis.scrollAlign` 这个字段没人读，必须改传 `updateData({scrollPosition: ...})`
- uCharts 的抽稀拿 `labelCount` 当「一屏想放几个」，且**不看 `enableScroll`**
- 对比图默认的 `right` 是给时间轴用的，用在这儿会把第一名顶到屏幕外

### 首页卡片相关

- 写 50% 会把间距也算进两列，第二列被挤到下一行
- 老 iOS 的 WebView 不认 `aspect-ratio`，卡片会压成一条
- 两个 flex 容器各写一份「项宽 + 间距 + 居中」，两份独立的账差一像素，七列下来就错开小半格
- `utils/date.js` 的一周起点是周一，而卡片上是「日历」从周日排

### 主题相关

- 手抄一份 `PRESET_KEYS` 清单，以后往调色板里加一项，老预设会悄悄少掉那一项
- 路径是导出那台手机的，本机打开是坏的
- 导入备份时漏抹一个预设的 `image`，就是个空路径
- `app.json` 的 `darkmode` 若被去掉会静默退化成「永远深色」

### 底栏相关

- 切到当前 tab 框架不会重跑 `onShow`，pending 标记会一直躺着
- 「页面滑动过之后底栏点不动」那个坑对加号一样成立（共用 `dispatch()`）
- 「新建习惯」高出去就会顶高胶囊，`app.wxss` 里 `.page` 的 `padding-bottom` 就压不住
- 连着猛点几个 tab，定时器被后一次覆盖，「后一次说了算」

### 取景相关

- 页面里不再有可拖的取景框，取景台收进正中的「调整背景图」弹窗
- 取景台太大，外面套一层 `width: 68%` 的框，必须套在外
- 取景弹窗打开时量两次舞台，否则量到的是缩放后的宽度