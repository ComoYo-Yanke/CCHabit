/*
 * 自定义 tabBar —— 悬浮在底部的胶囊。
 *
 * 注意：自定义 tabBar 由框架独立挂载，继承不到 page 上的 CSS 变量，
 * 所以底色 / 描边 / 选中态填充由 index.js 用行内样式写（跟主题走），
 * 这里保留的值只是行内样式缺失时的兜底，不是唯一来源。
 *
 * 胶囊是**浮在内容之上**的，不占布局 —— 页面底部要让出它的高度，
 * 那份 padding 写在 app.wxss 的 .page 里，两处改一处就得改另一处。
 * 当前高度：离底 20rpx + 胶囊 108rpx（10rpx 内边距 ×2 + tab 86rpx + 描边 2rpx）。
 * 左边那枚「新建习惯」圆钮也是 86rpx —— 胶囊高度由最高的孩子定，它高出去就会顶高胶囊。
 *
 * 图标**跟着主题走**（也走行内样式，见 index.js）：其余图标仍是烘焙的固定色
 * （深色 / 白色底上都有 4.6:1 以上对比度），但底栏这里不行 ——
 * 选中胶囊的填充色就是主色，图标再固定成 #5B8CFF，自定义主题下两者会当场打架。
 * 深 / 浅色两套取的仍是原本那两个烘焙色，所以那两套的观感和以前一致。
 */
.tabbar {
  position: fixed;
  left: 50%;
  /* 定宽由内容决定，所以要自己居中。transform 落在这个 fixed 元素**自身**上没问题：
     它内部没有别的 fixed 后代，不会凭空多出一个包含块（对比 README 决策 15） */
  transform: translateX(-50%);
  /* 离底距离。先写兜底值再写 calc：env() 参与 calc 若解析失败整条声明会被丢弃，
     那样胶囊会贴到屏幕最下沿，在全面屏上正好压住 home indicator */
  bottom: 20rpx;
  bottom: calc(20rpx + env(safe-area-inset-bottom));
  z-index: 900;

  display: flex;
  align-items: center;
  padding: 10rpx;
  border-radius: 999rpx;
  border: 1rpx solid #1F242F;
  background: #161A22;
  box-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.16), 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
  /*
   * 这里**不用 backdrop-filter** 做毛玻璃，虽然它更好看。
   * 原因不是性能，是点击：底栏是 position: fixed，而 backdrop-filter 会让它
   * 变成一个独立合成层；页面滚动之后这个层的命中区域会和实际位置脱节，
   * 表现就是「滑动过页面后底栏点不动、点几次才生效」。
   * 底色本身已经是 0.88 的透明度（见 utils/theme.js 的 tabbarBg），
   * 去掉模糊后观感几乎不变，换来的是点击始终可靠。
   * 若日后确认点击稳定，想加回毛玻璃只需恢复这两行。
   */
}

/* 新建习惯的圆钮，在「主页」左边。
   高度必须和 .tab 一样是 86rpx（20+46+20）：胶囊的高度就是最高的那个孩子定的，
   这里要是高出去，整个胶囊会变高，app.wxss 里 .page 的 padding-bottom 就压不住了。
   margin-right 和 .tab 之间的间距对齐（.tab 自己没有横向 margin）。
   底色由行内样式给（实心主色，跟主题走，见 index.js 的 syncTheme） */
.tab-add {
  width: 86rpx;
  height: 86rpx;
  border-radius: 999rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  /* 缩不缩都行，但和右边三个 tab 贴太紧会看起来像一个整体，留一点缝 */
  margin-right: 8rpx;
  flex-shrink: 0;
  transition: background-color 0.18s ease;
}

.tab-add-icon {
  width: 44rpx;
  height: 44rpx;
}

.tab-add:active {
  opacity: 0.85;
}

.tab {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20rpx 36rpx;
  border-radius: 999rpx;
  /* 选中态的背景色由行内样式给（颜色跟主题走，见 index.js 的 syncTheme）。
     这里只负责让它跳得软一点 —— 切换时页面在淡出，高亮跟着一起换段，
     eased 一下比硬切自然。 */
  transition: background-color 0.18s ease;
}

/* 图标本身不在这里定色：底色 / 描边 / 选中态填充 / 图标颜色全部由行内样式给
   （见 index.js 的 syncTheme），这里只负责尺寸和铺法。
   尺寸和 .ic 一致（44rpx 那套是 icons.wxss 的默认值，这里略微放大到 46rpx） */
.tab-icon {
  width: 46rpx;
  height: 46rpx;
  background-repeat: no-repeat;
  background-position: center;
  background-size: 100% 100%;
}
